namespace GameConstants
{
    public static class IngameConstants
    {
        // if the grid is within this limit, snap to the position.
        public readonly static float SNAP_THRESHOLD = 0.3f;
        public readonly static bool STOP_DRAG_AFTER_SNAP = true;
    }
}